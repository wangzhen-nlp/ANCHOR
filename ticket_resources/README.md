# ticket_resources

这里集中放和工单输入直接相关的默认资源文件。

当前默认约定包括：

- `Incident Ticket_20260201-20260318.xlsx`

说明：

- 这些文件目前只是默认引用位置，仓库里不强制提交实际数据。
- 相关脚本已经统一改成优先使用这里的默认路径。
- 如果 `ticket_resources/` 下没有对应文件，但仓库根目录或旧的 `topology_resources/` 下仍有旧同名资源，当前也会自动兼容旧位置。
- 仍然可以通过命令行参数覆盖为其它路径。
